# وب‌اسکرپر سادهٔ صفحات HTML

فایل [`scripts/simple_web_scraper.py`](scripts/simple_web_scraper.py) یک ابزار خط فرمانِ بدون وابستگی خارجی است که یک URL را می‌گیرد، صفحهٔ آغاز و صفحه‌های HTML پیوندشده در محدودهٔ آن را روی رایانه ذخیره می‌کند.

> فقط وب‌سایت‌هایی را دانلود کنید که اجازهٔ دسترسی و بایگانی آن‌ها را دارید. ابزار به `robots.txt` احترام می‌گذارد و در صورت ناموفق بودن دریافت آن (به‌جز ۴۰۴) خزش را شروع نمی‌کند. این ابزار هیچ ورود به حساب، دورزدن محدودیت یا اجرای JavaScript انجام نمی‌دهد.

## پیش‌نیاز

Python 3.9 یا جدیدتر کافی است؛ نصب `pip` یا کتابخانهٔ دیگری لازم نیست.

## استفادهٔ پایه

از ریشهٔ مخزن اجرا کنید:

```bash
python3 scripts/simple_web_scraper.py "https://example.org/docs/" --output web_archive
```

خروجی پیش‌فرض در پوشهٔ `web_archive/` قرار می‌گیرد. ساختار URL تا حد امکان حفظ می‌شود؛ مثلاً صفحهٔ `/docs/guide.html` در مسیری مشابه `web_archive/example.org/docs/guide.html` ذخیره خواهد شد. فایل `web_archive/manifest.json` هم نگاشت URLها به فایل‌ها، وضعیت خطاها و تعداد صفحه‌های ذخیره‌شده را دارد.

## محدودهٔ خزش

خزش هرگز از مبدأ URL (scheme، دامنه و درگاه) خارج نمی‌شود.

- حالت پیش‌فرض `--scope path` فقط خود صفحه و مسیر زیر آن را دنبال می‌کند. برای نمونه اگر URL آغاز `https://example.org/docs/intro.html` باشد، پیوندهای `/docs/...` بررسی می‌شوند، نه `/news/...`.
- `--scope site` همهٔ پیوندهای همان مبدأ را، با رعایت سایر حدها، دنبال می‌کند:

  ```bash
  python3 scripts/simple_web_scraper.py "https://example.org/" \
    --scope site --output archive_example
  ```

پیوندهای بیرونی، `mailto:`، `tel:`، fragmentهای `#...` و محتوای غیر HTML ذخیره نمی‌شوند. fragmentها در URL تکراری ایجاد نمی‌کنند، اما query stringها (مانند `?page=2`) حفظ می‌شوند، چون ممکن است صفحه‌های متفاوتی باشند.

## کنترل حجم و سرعت

برای جلوگیری از فشار ناخواسته به سرور، مقدارهای پیش‌فرض این‌هاست: حداکثر ۲۵۰ صفحه، عمق پیوند ۸، مکث حداقل ۰٫۵ ثانیه بین درخواست‌های صفحه و حداکثر ۱۰ مگابایت برای هر پاسخ HTML.

```bash
python3 scripts/simple_web_scraper.py "https://example.org/docs/" \
  --output archive_docs \
  --max-pages 80 \
  --max-depth 4 \
  --delay 1.0 \
  --timeout 30 \
  --max-file-mb 5
```

- `--max-pages 0`: بدون محدودیت تعداد صفحه. فقط برای سایتی که مجوز و اندازهٔ آن را می‌شناسید استفاده کنید.
- `--max-depth 0`: فقط صفحهٔ آغاز.
- `--user-agent "نام ابزار شما"`: نام ابزار ارسالی در درخواست‌ها را تغییر می‌دهد.
- `--help`: همهٔ گزینه‌ها و مقدار پیش‌فرض‌شان را نمایش می‌دهد.

## محدودیت‌های آگاهانه

این یک خزندهٔ سبک برای HTML ایستا است؛ صفحات ساخته‌شده با JavaScript، فرم‌ها، داده‌های پشت ورود، PDF، تصویر، CSS و JavaScript را واکشی نمی‌کند. فایل‌های HTML به‌شکل خام ذخیره می‌شوند و پیوندهای داخل آن‌ها برای حالت آفلاین بازنویسی نمی‌شوند؛ برای پیدا کردن هر صفحه از `manifest.json` استفاده کنید.

اگر `robots.txt` یک URL را منع کند، یا یک تغییرمسیر به دامنه/مسیر خارج از محدوده برود، آن URL در گزارش با وضعیت `skipped` ثبت می‌شود. خطای یک صفحه نیز باعث از دست‌رفتن صفحه‌های دیگر نمی‌شود و در همان گزارش ثبت خواهد شد.
